{% extends('base.html.twig') %}

{% block title %}Show!{% endblock %}

{% block body %}
    <h2>{{ post.id }}</h2>
    <h1>{{ post.title }}</h1>
    <img src="{{ '/uploads/' ~ post.image }}" alt="">
{% endblock %}